import { styled } from '@mui/material/styles';
import { Tabs, Tab } from '@mui/material';

const StyledTab = styled(Tab)(({ theme }) => ({
  fontSize: '16px',
  color: 'black',
  '&.Mui-selected': {
    color: theme.palette.primary.main,
    fontWeight: 'bold',
  },
}));

<Tabs value={value} onChange={handleChange} aria-label="tabs">
  <StyledTab label="Interest Rate - Daily" />
  <StyledTab label="Interest Rate - Cycle" />
  <StyledTab label="Interest Rate - Reports" />
  <StyledTab label="Interest Rate Maintenance" />
</Tabs>
