package com.mycompany.jai.backend.controller;

import org.springframework.boot.web.error.ErrorAttributeOptions;
import org.springframework.boot.web.servlet.error.ErrorController;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

@Controller
public class SpaIndexController implements ErrorController {

    // Catch-all for /console and react routes
    @RequestMapping({"/console", "/console/**"})
    public String forwardConsole() {
        return "forward:/console/index.html";
    }

    // Handle direct refreshes and unknown routes under /console
    @RequestMapping("/error")
    public String handleError(HttpServletRequest request, HttpServletResponse response) {
        // Only forward browser GETs — let API or other errors process normally
        return request.getMethod().equalsIgnoreCase("GET")
            ? "forward:/console/index.html"
            : null;
    }
}
