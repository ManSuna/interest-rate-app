<Button variant="contained" onClick={() => setShowForm((prev) => !prev)}>
  {showForm ? 'Cancel' : 'Add Interest Rate'}
</Button>


<Button variant="contained" onClick={() => setShowForm((prev) => !prev)}>
  Add Interest Rate
</Button>
